@model List<FrontEnd.Models.ViewModels.PoliciaViewModel>

<table class="table">

    <thead class="thead-light">
        <tr>
            <th scope="col">Cédula</th>
            <th scope="col">Nombre</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@Html.DisplayFor(modelItem => item.Cedula)</th>
                <td>
                    <label><a href="#" onclick="reply_click(this.id, this.name)" id="@item.Cedula" name="@item.Nombre"> @item.Nombre</a></label>
                </td>
            </tr>
        }
    </tbody>
</table>